## Concept

### Definition

### Why it exists

### Example

### JavaScript Equivalent

### Common Mistake

### My Observation

###

### day 1

print(f"Hello {name}") this is how template literals works in python same as `` in js

### day2

age = int(input('input age:')) # parse int before the input to get the type int as age is int not 'str' as that is what we get in input as it takes str

print(type(age))
age2 = int(input("Age: "))
if we use age as int which we should but try to add str to it it will not work as the types are different but
in case type is 'str' and adding 'str' to it it will add to the suffix i.e. '23'+'1' = 231 not 24
thats why we use types so that 'int' will and int also note we can't add 23+'1' it must be 23+1 =24
